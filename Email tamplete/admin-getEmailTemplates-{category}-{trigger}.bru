meta {
  name: /admin/getEmailTemplates/{category}/{trigger}
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/admin/getEmailTemplates/SALES/LEAD
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
