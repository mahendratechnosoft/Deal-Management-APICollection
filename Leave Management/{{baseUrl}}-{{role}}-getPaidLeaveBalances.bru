meta {
  name: {{baseUrl}}/{{role}}/getPaidLeaveBalances
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/{{role}}/getPaidLeaveBalances
  body: none
  auth: inherit
}

params:query {
  ~leaveType: Sick Leaves
}

settings {
  encodeUrl: true
  timeout: 0
}
