meta {
  name: {{baseUrl}}/{{role}}/getOptions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getOptions?category=STUDENT_LEAD_STATUS&search=&active=true
  body: none
  auth: inherit
}

params:query {
  category: STUDENT_LEAD_STATUS
  search: 
  active: true
  ~sort: sequence,asc
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
      LEAD_TYPE,
      LEAD_SOURCE,
      LEAD_STATUS,
      INDUSTRY,
      STUDENT_LEAD_TYPE,
      STUDENT_LEAD_SOURCE,
      STUDENT_LEAD_STATUS,
}
