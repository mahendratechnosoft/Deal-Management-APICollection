meta {
  name: {{baseUrl}}/{{role}}/getOptions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getOptions?category=TODO_LIST_CATEGORY
  body: none
  auth: inherit
}

params:query {
  category: TODO_LIST_CATEGORY
  ~search: 
  ~active: true
  ~sort: sequence,asc
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
      LEAD_TYPE,
      LEAD_SOURCE,
      LEAD_STATUS,
      INDUSTRY,
      STUDENT_LEAD_TYPE,
      STUDENT_LEAD_SOURCE,
      STUDENT_LEAD_STATUS,
}
