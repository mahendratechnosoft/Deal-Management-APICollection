meta {
  name: {{baseUrl}}/admin/getAmcById/{amcId}
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/{{role}}/getAmcById/{amcId}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
