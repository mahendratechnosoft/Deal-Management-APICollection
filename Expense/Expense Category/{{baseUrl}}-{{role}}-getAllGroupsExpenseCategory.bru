meta {
  name: {{baseUrl}}/{{role}}/getAllGroupsExpenseCategory
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/{{role}}/getAllGroupsExpenseCategory?type=direct
  body: none
  auth: inherit
}

params:query {
  type: direct
}

settings {
  encodeUrl: true
  timeout: 0
}
