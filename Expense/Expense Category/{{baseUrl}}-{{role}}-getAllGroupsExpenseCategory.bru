meta {
  name: {{baseUrl}}/{{role}}/getAllGroupsExpenseCategory
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/{{role}}/getAllGroupsExpenseCategory
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
