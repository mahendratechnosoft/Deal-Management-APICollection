meta {
  name: {{baseUrl}}/{{role}}/getTicketForDropdown
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/{{role}}/getTicketForDropdown
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
