meta {
  name: {{baseUrl}}/{{role}}/getAllTicketStatus
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getAllTicketStatus
  body: none
  auth: inherit
}

params:query {
  ~search: open
}

settings {
  encodeUrl: true
  timeout: 0
}
