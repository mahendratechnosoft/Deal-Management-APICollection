meta {
  name: {{baseUrl}}/{{role}}/createTicketNote
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{role}}/createTicketNote
  body: json
  auth: inherit
}

body:json {
  {
    "ticketId": "c0a80130-9e44-1957-819e-44cc05c30004",
    "note": "this is my first note"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
