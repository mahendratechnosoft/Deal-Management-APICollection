meta {
  name: {{baseUrl}}/{{role}}/createTicketPriority
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{role}}/createTicketPriority
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Normal"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
