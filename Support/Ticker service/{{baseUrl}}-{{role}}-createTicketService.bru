meta {
  name: {{baseUrl}}/{{role}}/createTicketService
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{role}}/createTicketService
  body: json
  auth: inherit
}

body:json {
  {
    "name":"Crm Technology"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
