meta {
  name: {{baseUrl}}/{{role}}/getVendorNameAndId
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/{{role}}/getVendorNameAndId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
