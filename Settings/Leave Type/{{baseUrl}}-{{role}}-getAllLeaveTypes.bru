meta {
  name: {{baseUrl}}/{{role}}/getAllLeaveTypes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{role}}/getAllLeaveTypes
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
