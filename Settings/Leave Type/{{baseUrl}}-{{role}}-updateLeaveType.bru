meta {
  name: {{baseUrl}}/{{role}}/updateLeaveType
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{role}}/updateLeaveType
  body: json
  auth: inherit
}

body:json {
  {
    "name":"Demo Leave",
    "description":"demo leave",
    "daysAllowedPerYear":"12",
    "isPaidLeave":false
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
