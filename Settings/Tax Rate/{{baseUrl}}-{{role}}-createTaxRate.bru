meta {
  name: {{baseUrl}}/{{role}}/createTaxRate
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/{{role}}/createTaxRate
  body: json
  auth: inherit
}

body:json {
  
  {
    "taxRateName":"IGST",
    "taxRate":10
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
