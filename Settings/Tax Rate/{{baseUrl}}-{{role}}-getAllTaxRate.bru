meta {
  name: {{baseUrl}}/{{role}}/getAllTaxRate
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{role}}/getAllTaxRate
  body: none
  auth: inherit
}

params:query {
  ~active: 
}

settings {
  encodeUrl: true
  timeout: 0
}
