meta {
  name: {{baseUrl}}/{{role}}/getStudentLeadsSetting
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getStudentLeadsSetting
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
