meta {
  name: {{baseUrl}}/{{role}}/getAllInvoiceServices
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/{{role}}/getAllInvoiceServices
  body: none
  auth: bearer
}

params:query {
  ~search: CAM
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
