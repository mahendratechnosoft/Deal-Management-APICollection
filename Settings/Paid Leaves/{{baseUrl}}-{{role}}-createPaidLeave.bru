meta {
  name: {{baseUrl}}/{{role}}/createPaidLeave
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{role}}/createPaidLeave
  body: json
  auth: inherit
}

body:json {
  {
    "type":"Sick Leave",
    "noOfLeaves":12
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
