meta {
  name: {{baseUrl}}/{{role}}/getStudentLeadStatusAndCount
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/{{role}}/getStudentLeadStatusAndCount
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
